import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.volleymotion',
  appName: 'volleymotion',
  webDir: 'dist/apps/volleymotion',
  bundledWebRuntime: false,
};

export default config;
